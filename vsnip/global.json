{
  "date": {
    "prefix": "date",
    "body": ["${CURRENT_YEAR}-${CURRENT_MONTH}-${CURRENT_DATE}}"],
    "description": "Simple date stamp"
  },
  "diso": {
    "prefix": "datetime",
    "body": ["${CURRENT_YEAR}-${CURRENT_MONTH}-${CURRENT_DATE}-${CURRENT_HOUR} ${CURRENT_MINUTE}:${CURRENT_SECOND}"],
    "description": "Simple date time stamp"
  }
}
